const { Router } = require('express');
const { Gender } = require('../db');
// aca me tengo que traer los metodos
const router = Router();


// aca tengo que armar mis metodos a /gender

router.get('/', async (req, res) => {

});


router.post('/', async (req, res) => {

});


module.exports = router;